/**
 * Test setup for server-integration package
 */

import { beforeEach, afterEach } from 'vitest';

// Setup test environment
beforeEach(() => {
  // Reset any global state
});

afterEach(() => {
  // Cleanup after each test
});
