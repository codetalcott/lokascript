name: Patterns Reference CI

on:
  push:
    paths:
      - 'packages/patterns-reference/**'
      - '.github/workflows/patterns-reference.yml'
  pull_request:
    paths:
      - 'packages/patterns-reference/**'
      - '.github/workflows/patterns-reference.yml'

jobs:
  test:
    name: Test & Validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        working-directory: packages/patterns-reference
        run: npm run typecheck

      - name: Run tests
        working-directory: packages/patterns-reference
        run: npm run test:run

      - name: Populate database
        working-directory: packages/patterns-reference
        run: npm run populate

      - name: Validate translations
        working-directory: packages/patterns-reference
        run: npx tsx scripts/validate-all.ts --fix

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build package
        working-directory: packages/patterns-reference
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: patterns-reference-dist
          path: packages/patterns-reference/dist/
          retention-days: 7
