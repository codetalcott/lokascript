/**
 * Value Extractor Types — Re-exported from @lokascript/framework
 *
 * These types are the canonical interfaces for the pluggable extractor system.
 * Semantic package consumers can import them from here for convenience.
 */

export type { ExtractionResult, ValueExtractor } from '@lokascript/framework';
