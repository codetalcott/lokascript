/**
 * French Increment Patterns
 *
 * Relying on auto-generated patterns from profile.
 * Empty stub follows toggle/ja model for simpler implementation.
 */

import type { LanguagePattern } from '../../types';

/**
 * Get French increment patterns.
 */
export function getIncrementPatternsFr(): LanguagePattern[] {
  return [];
}
